import { Provider } from '@angular/core';

/**
 * Feature-level providers (kept for consistent feature structure).
 * Add Http interceptors, feature services, etc. here if needed.
 */
export const authProviders: Provider[] = [];


